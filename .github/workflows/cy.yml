name: TEST
on:
    push:
        branches:
            - main

jobs:
    test_mysql:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Run Unit test
              run: python3 test.py