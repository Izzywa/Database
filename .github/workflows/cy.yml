name: TEST
on:
    push:
        branches: [ "main" ]

jobs:
    Test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Run Unit test
              run: echo Hello, world!