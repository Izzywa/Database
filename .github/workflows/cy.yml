name: TEST
on:
    push:
        branches: [ "main" ]

jobs:
    Test:
        runs-on: ubuntu-latest
        env: 
            DB_USER: ${{secrets.DB_USER}}
        steps:
            - uses: actions/checkout@v4
            - name: Test database
              run: pipx install virtualenv